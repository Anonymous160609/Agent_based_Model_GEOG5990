import sys
  
print("test for model from:", sys.argv[0])
  
print("Argument List: model: agent number; iteration number; other", str(sys.argv))

print("This is the name of the program:",
       sys.argv[0])
print("Number of elements including the name of the program:",
       len(sys.argv))
print("Number of elements excluding the name of the program:",
      (len(sys.argv)-1))
print("Argument List:",
       str(sys.argv))

add = 0.0
  
# Getting the length of command
# line arguments

print ("para input", sys.argv[0], sys.argv[1], sys.argv[2], sys.argv[3])
  
  
print ("the sum is :", add)